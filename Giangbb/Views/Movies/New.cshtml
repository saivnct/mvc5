@using Giangbb.Models
@model Giangbb.ViewModels.NewMovieViewModel
@{
    ViewBag.Title = "New";
    Layout = "~/Views/Shared/_Layout_MaterialDashboard.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-offset-3 col-md-6">
            <div class="card">
                <div class="card-header card-header-icon" data-background-color="rose">
                    <i class="material-icons">contacts</i>
                </div>
                <div class="card-content">
                    <h4 class="card-title">Movie</h4>
                    @using (Html.BeginForm("Add", "Movies", FormMethod.Post, new { id = "movieForm" }))
                    {
                        @Html.ValidationSummary() //show all error

                        <div class="form-group">
                            <label for="MovieForm_Name">Name</label>
                            <input type="text" class="form-control" name="MovieForm.Name" id="MovieForm_Name">
                        </div>


                        <div class="form-group">
                            <label for="MovieForm_ReleaseDate">Release Date</label>
                            <input type="text" class="form-control datepicker" name="MovieForm.ReleaseDate" id="MovieForm_ReleaseDate" />
                        </div>



                        <select class="selectpicker" name="MovieForm.GenreId" id="MovieForm_GenreId" data-size="4" data-style="select-with-transition" title="Select Genre">
                            <option disabled>Select Genre</option>
                            @foreach (var genre in Model.Genres)
                            {
                                <option value="@genre.Id">@genre.Name</option>
                            }
                        </select>


                        <div class="form-group">
                            <label for="MovieForm_NumberInStock">Number In Stock</label>
                            <input type="text" class="form-control" name="MovieForm.NumberInStock" id="MovieForm_NumberInStock">
                        </div>


                        @Html.AntiForgeryToken()

                        <div class="row">
                            <div class="col-md-offset-5 col-md-6">
                                <div class="form-group form-button">
                                    <button type="submit" class="btn btn-fill btn-rose">Save</button>
                                </div>

                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section AddToBotom{
    <script src="@System.Web.Optimization.BundleTable.Bundles.ResolveBundleUrl("~/View/movies/new/js", true)"></script>
}

